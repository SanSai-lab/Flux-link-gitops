apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: azure-keyvault-e2e-policy-uat
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: azure-keyvault-e2e-uat
  policy:
    semver:
      range: "1.0.x"
